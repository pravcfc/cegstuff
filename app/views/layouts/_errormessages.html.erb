<% if @current_user.errors.any? %>
  <div id="updateform_errors">
    <h3><%= pluralize(@current_user.errors.count, "error") %> in the form : </h3>
    <ul>
    <% @current_user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
</center>
